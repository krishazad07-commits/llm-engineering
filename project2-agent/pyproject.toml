[project]
name = "project2-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "krishazad07-commits", email = "krishazad07@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "groq>=1.7.0",
    "psycopg[binary]>=3.3.6",
    "python-dotenv>=1.2.3",
]

[project.scripts]
project2-agent = "project2_agent:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
